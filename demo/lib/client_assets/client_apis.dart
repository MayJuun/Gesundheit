/// Dev endpoint for server
const devUrl = 'https://mayjuun.com/dev';

/// Stage endpoint for server
const stageUrl = 'https://mayjuun.com/stage';

/// Prod endpoint for server
const prodUrl = 'https://mayjuun.com/prod';

/// These are because usually you cannot link directly to the server so a proxy
/// endpoint must be provided and used
const devProxy = 'https://mayjuun.com/dev/proxy';

const stageProxy = 'https://mayjuun.com/stage/proxy';

const prodProxy = 'https://mayjuun.com/prod/proxy';

/// ClientIds for the app for the 3 different production levels
const devId = 'dev';

const stageId = 'stage';

const prodId = 'prod';

/// The client name (computer readable)
const clientAppName = 'mpepu';

/// The client (App) name (human readable)
const clientAppTitle = 'Mpepu';
